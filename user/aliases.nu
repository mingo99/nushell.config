alias ps = procs

# directories
# alias ls = lsd
alias ll = ls -l
alias la = ls -a
alias lla = ls -la
alias lt = lsd --tree
alias md = mkdir
alias rd = rmdir

alias t = tmux
alias p = pfetch

alias cat = bat
alias jso = joshuto
alias lg = lazygit
alias fzf = fzf --preview 'bat --color=always {}' --preview-window '~3'
