alias ps = procs

# directories
alias ls = exa --icons
alias ll = ls -l
alias la = ls -a
alias lla = ls -la
alias md = mkdir
alias rd = rmdir

alias cat = bat
alias jso = joshuto
alias lg = lazygit
alias fzf = fzf --preview 'bat --color=always {}' --preview-window '~3'
